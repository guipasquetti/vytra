# (arquivo aposentado)

O conteúdo deste documento foi devolvido para o `HANDOFF.md` na raiz do repositório, que é a
fonte canônica única do projeto.

Motivo: este arquivo duplicava decisões e estado de infraestrutura que já viviam no handoff, e
em poucas horas as duas versões divergiram — o desenho de DNS aqui dizia "app na raiz, servido
pelo EAS", enquanto o que foi de fato montado é landing na raiz pela Vercel e app em
`app.vytraoficial.com.br`. Duas fontes para o mesmo fato é como elas divergem.

**Regra, sem exceção: nunca duplicar o `HANDOFF.md`.** Codex e Claude compartilham aquele
arquivo. Documento separado só para assunto que o handoff não cobre, como `BRAND.md`, que são
regras de uso da marca.

Pode apagar este arquivo.
